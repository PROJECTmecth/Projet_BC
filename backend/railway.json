{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan optimize:clear && php artisan migrate --force && php -S 0.0.0.0:${PORT:-8080} -t public",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300
  }
}